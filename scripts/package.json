{
  "name": "analyze-bundle",
  "version": "1.0.0",
  "description": "Bundle size analysis for Lab02",
  "scripts": {
    "analyze": "npm run build && npm run analyze:size",
    "analyze:size": "node scripts/analyze-bundle.js",
    "build:analyze": "vite build --mode production",
    "visualize": "npx vite-bundle-visualizer"
  }
}
